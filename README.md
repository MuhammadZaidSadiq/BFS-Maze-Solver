# BFS-Maze-Solver
A Python-based GUI Maze Generator and Solver using Breadth-First Search (BFS).
A Python GUI project that generates a maze and solves it using Breadth-First Search (BFS). Includes live pathfinding visualization with Tkinter.
